# Copy this to .env and fill in real values. Never commit .env.
#
# In dev the Vite proxy (vite.config.js) handles /api and /auth, so VITE_API_URL
# is only consumed in production builds (Vercel in Phase 11).

VITE_API_URL=http://localhost:5000
VITE_SOCKET_URL=http://localhost:5000
